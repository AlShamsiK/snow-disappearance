# Colab already ships recent numpy, pandas, scipy, scikit-learn and matplotlib.
# Pinning exact versions forces slow downgrades, so require minimums instead and
# pin only what Colab does not ship. The versions actually used are recorded in
# the README under "Reproducibility"; the notebook's setup cell prints them.
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
matplotlib>=3.9
joblib>=1.4
requests>=2.32
pyarrow>=17
lightgbm==4.5.0
