__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store
.venv/
env/
*.egg-info/

# Keep data/ and models/ tracked on purpose: the project is reproducible from a
# clean clone and the Colab demo must not depend on the NRCS service.
# Exclude only scratch files.
data/scratch/
*.tmp

# Regenerated by the notebook in about a minute; too large to version.
data/processed/daily_clean.csv.gz
